# 中国城市历年气温对比 — 复检缺陷分析（P0 修复后）

> 复检范围：当前 `main` 分支全部源码（`src/` 共 10 个文件，约 1550 行）。
> 复检动作：逐文件通读 + `npm run build` 验证 + Open-Meteo `02-29` 边界实测。
> 结论：**P0（数据正确性）问题已全部修复且验证通过**；剩余缺陷集中在功能完整性、健壮性、体验与工程整洁度（P1/P2），均为非阻断项。

---

## 一、已确认修复（P0 回顾，本轮复核无误）

| 编号 | 问题 | 现状 |
|---|---|---|
| P0-1 | 跨年区间整段走 forecast / 未来段报错致整年曲线消失 | 已拆分"起始年段+次年段"，历史走 archive、未来走 forecast，并做 horizon 收敛 + try/catch 容错 |
| P0-2 | 闰年/平年对齐错误（按索引对齐） | 改为 **MM-DD 对齐**，`buildMonthDayLabels` 用闰年参考年(2000)生成标签，平年 `02-29` 自然为 null |
| P0-3 | 当年曲线在选到 15 天外的预报日期时整条消失 | horizon 收敛到约 15 天 + 容错，超出部分部分显示并加提示 |
| P0-4 | 详细信息日期列为空 | 改为直接用 `labels[i]`（原 `formatMonthDay` 二次截取致空） |
| P0-5 | 跨年区间缺少年份分隔 | 在 `12-xx → 01-xx` 过渡处插入 `.year-divider` 分隔行 |

构建验证：`tsc && vite build` 通过，无类型/编译错误。

---

## 二、仍存在的 P1 缺陷（功能 / 健壮性，建议优先）

### P1-1 城市选择仅在"点击下拉项"时生效（陷阱）
`src/components/CitySearch.ts`：输入文字后只有点击下拉项才触发 `onSelect` 更新 `state.city`。
- **现象**：用户搜索并看到候选城市，但未点击、直接按"查询"→ 实际仍用**旧城市**（默认大连或上次所选）请求；无任何提示。
- **影响**：静默用错城市，比对结果失真且用户不易察觉。
- **建议**：输入框 `blur`/`Enter` 时若下拉有唯一/首个匹配项则自动选用；或在查询前校验"输入框文本 ≠ 当前已选城市"并给出提醒。

### P1-2 年份选择硬限制（10 个 / 仅近 20 年）
`src/components/YearSelector.ts`：`getRecentYears(20, false)` 只给近 20 年，且 `selectedYears.size >= 10` 强制拦截。
- **现象**：无法对比 30 年趋势，也无法选 1998、2010 等特定历史年份。
- **建议**：上限放宽到 15–20，并允许手动输入任意年份（带校验）。

### P1-3 缺少统计摘要面板
当前只有折线图 + 明细表，没有**年均/最高/最低/距平（偏离多年均值）**等汇总。
- **影响**：工具名曰"对比"，但缺少"哪一年异常偏暖/偏冷"的量化工况，价值未充分发挥。
- **建议**：在图表下方加一行每列（年份）的均值、极差，并标注相对多年均值的距平。

### P1-4 无状态持久化 / 分享
刷新页面即回到默认（大连、近 ±10 天、近 5 年），选择不可复现、不可分享。
- **建议**：将 `city / 起止 MM-DD / years / tempType` 写入 URL query（或 localStorage），加载时回填。

### P1-5 无客户端缓存
每次查询对每一年最多发 2 次 archive + 2 次 forecast（10 年≈20–40 请求），重复条件不复用。
- **影响**：重复查询浪费额度；Open-Meteo 限流时易致部分年份失败（虽已被 try/catch 兜住，但体验差）。
- **建议**：以 `city+年份+起止+数据源(archive/forecast)` 为键做内存/IndexedDB 缓存（历史数据几乎不变，天然适合缓存）。

---

## 三、P2 缺陷（体验 / 工程整洁度）

| 编号 | 位置 | 问题 | 建议 |
|---|---|---|---|
| P2-1 | `helpers.ts` `assignColorsByAverageTemp` | 只用**最高气温**均值排序上色；切到"最低气温"tab 颜色不变；**仅选 1 年**时 `ratio=0` 恒为红色（最热色），与真实温度无关，误导 | 上色应按**当前 tempType** 计算；单年时用温度区间映射而非"排名" |
| P2-2 | `DataTable.ts` `getTemperatureColor` | 仅 `≥35 / ≥30` 两档上色，冬季全灰；与折线 10 色板无关，"冷=紫"配色意图从未在表格体现 | 与折线统一一套冷暖色阶，或按距平上色 |
| P2-3 | `TempChart.ts` | 预报点用菱形+虚线，但图例 `pointStyle:'circle'` 不体现；tooltip 不区分预报/实测 | 图例加标注；tooltip 命中预报点时补"（预报）" |
| P2-4 | `DataTable.ts` 分隔行文案 | 写死"以下为次年 1–2 月"，若区间到 3 月（如 11-15→03-15）文案不准 | 文案按实际 `endMonthDay` 月份动态生成 |
| P2-5 | `main.ts` truncated 提示 | 当年冬季尚未到来（跨年区间中当年段全在未来）时整年无数据，仍提示"已按可获取范围部分显示"，易误解为"有数据只是少了" | 区分"部分截断"与"当年整段在未来无数据"两种文案 |
| P2-6 | 各输入控件 | `input` 仅 placeholder、无关联 `<label for>`；图表 canvas 无 aria/文本备选 | 加 label + aria-label，提升可访问性 |
| P2-7 | 长区间 X 轴 | 全年区间 366 个刻度，标签拥挤 | 按区间长度自适应抽稀 tick（如每 7/15 天一个） |
| P2-8 | `helpers.ts` | 死代码 `formatMonthDay` 仅定义未使用（P0-4 修复后遗留） | 删除 |
| P2-9 | `main.ts` `handleQuery` | 快速连点"查询"会并发执行两次 `handleQuery`，可能交叉刷新 `state` | 查询中禁用按钮 / 用 token 丢弃过期结果 |
| P2-10 | `DateRangePicker.ts` | `type="date"` 显示完整日期，但只用 MM-DD，"年"无意义，用户易误以为能选特定年份区间 | 改用两个 `type="month"` 或自定义 MM-DD 选择器，明确"同年对齐"语义 |

---

## 四、P0 修复引入的边角问题（轻微，非阻断）

1. **跨年 + 当前年的"当年整段空白"**：跨年区间下，当前年的"起始年段/次年段"往往整段落在未来（如 12-01→02-28 在 7 月查询），Open-Meteo forecast 对 >15 天返回 400，被容错跳过 → 当年整年 null。这是**语义正确**的（当年冬季还没到），但配合 P2-5 的提示文案会略显误导，已归并到 P2-5。
2. **`02-29` 边界实测**：以平年 `2023-02-29` 作为 start/end 调 archive，**返回 HTTP 200 且静默跳过该非法日**（返回序列直接跳到 `03-01`，无报错）。因此："以闰年 02-29 为区间端点 → 平年该天显示 null"这一行为**正确**；唯一副作用是 Open-Meteo 会把非法 `02-29` 归一化为 `03-01` 返回，仅在该极端边界出现轻微对齐噪声，不致崩溃。列为**已知边角，低优先**，无需立即处理。

---

## 五、优先级建议（下一步）

1. **先做 P1-1（城市选择陷阱）+ P1-3（统计摘要）+ P1-4（URL 持久化）**：投入小、体验/价值提升最大。
2. **再做 P1-2 / P1-5**：放开年份限制 + 加缓存，支撑长期气候对比。
3. **P2 随版本迭代清理**：重点 P2-1/P2-2（配色语义）、P2-3（预报标识）、P2-8（死代码）。

> 说明：以上为分析与建议，**未改动任何代码**。如需我落地其中某几项，告知优先级即可。
