# 缺陷分析报告

> 当前实现：Vite + TypeScript + ECharts，Open-Meteo 数据源
> 更新日期：2026-07-31

## 当前状态

本轮已完成：

- 查询使用输入快照、`AbortController` 和请求序号，旧响应不再覆盖新查询。
- 历史数据加入页面会话缓存，预报数据缓存 10 分钟。
- 分享 URL 保存城市名和经纬度；旧版仅城市名链接会通过地理编码兼容解析。
- 定位成功后自动重新查询，且用户手动选城优先于延迟返回的定位。
- 修复日期选择器无效默认日期及 URL 日期被默认值覆盖的问题。
- 统计改为逐年对比，不再跨所有年份累加。
- 预报失败明确提示；详情表标明最高/最低和预报数据。
- 图表已迁移至 ECharts，系列显隐按年份保存，日值曲线不使用平滑插值。
- 新增 Vitest 回归测试，覆盖闰日、跨年标签和逐年统计口径。

当前剩余风险：

| 优先级 | 项目 | 说明 |
|---|---|---|
| P2 | 区县坐标依赖第三方地理编码 | 少数区县可能退化到城市中心坐标，建议后续引入带行政区代码的坐标数据源。 |
| P2 | 无失败年份一键重试 | 当前会显示失败原因，用户需要再次点击查询。 |
| P2 | 主包体积较大 | 生产 JS 约 1.13 MB，移动网络下可继续做组件和数据分包。 |
| P3 | 无 CI | 已有本地测试脚本，但尚未配置自动执行 `npm test` 与 `npm run build`。 |
| P3 | 查询偏好不做本地持久化 | URL 已可复现成功查询；未查询的控件变化不会跨刷新保存。 |

---

## 历史审查记录（已归档）

以下内容基于旧版 Chart.js 实现，仅保留用于追溯，不代表当前代码状态。

---

## 0. 现状概述

已完成的功能模块：

| 模块 | 文件 | 说明 |
|---|---|---|
| 城市搜索 | `CitySearch.ts` | 中国城市地理编码搜索 |
| 日期范围 | `DateRangePicker.ts` | 起止月日选择，支持跨年区间 |
| 年份选择 | `YearSelector.ts` | 近 20 年候选，最多选 10 年 |
| 图表 | `TempChart.ts` | 多年温度曲线 + 所选年份均值虚线 + 图例切换显隐 |
| 统计卡 | `StatsCards.ts` | 8 项区间统计 + 逐年均值条 |
| 极端事件卡 | `ExtremeCards.ts` | 按年分组的热浪/寒潮卡片（含预报标记） |
| 检测逻辑 | `logic/extremes.ts` | 热浪/寒潮检测（国标量级阈值） |
| 统计逻辑 | `logic/stats.ts` | 多年逐日均值、区间汇总、逐年均值 |

**历史说明**：该版本已验证闰年 02-29 对齐、跨年日期归属和预报 horizon；图表相关描述已被当前 ECharts 实现取代。

---

## 1. 缺陷清单（按优先级）

### P1 — 高影响（功能缺口 / 正确性 / 稳定性）

| 编号 | 缺陷 | 位置 | 影响 | 建议 |
|---|---|---|---|---|
| **P1-1** | 并发查询无取消/竞态保护 | `main.ts:266-348`（`handleQuery`） | 用户快速连续点击「查询」或切换温度标签时，多个 `Promise.all` 同时写入 `state.yearlyData` 并触发渲染，后完成者覆盖先完成者，可能展示陈旧数据；无 `AbortController`。 | 用 `AbortController` 取消在途请求；用请求序号/令牌（递增 `queryToken`）丢弃过期响应。 |
| **P1-2** | 无本地缓存，重复请求 + 限流风险 | `api/open-meteo.ts`、`main.ts:194-263`（`fetchYearData`） | 每次查询都重新拉取全部年份；切换温度标签虽复用内存数据，但换城市/年份/区间即重拉。Open-Meteo 有速率限制，多年份夏季查询实测触发 **429**（见 E2E2）。 | 按 `(city, year, md-range)` 做内存/IndexedDB 缓存，命中则跳过网络；对 forecast 段单独缓存。 |
| **P1-3** | 无状态持久化 | 全局（grep 确认无 `localStorage`） | 刷新页面后城市/年份/区间选择全部丢失，默认城市大连 + 近 5 年重新加载。 | 将 `state` 关键字段序列化到 `localStorage`，启动时恢复。 |
| **P1-4** | 年份上限 10，且「全选」仅取近 10 年 | `YearSelector.ts:94`（`allYears.slice(-10)`）、`:49/:50/:76` | 候选 20 年但最多选 10 年；「全选」=`slice(-10)`。长期气候对比（如 20 年）受限，且暗示「全选」实际只选 10 年，文案有歧义。 | 放宽上限或明确说明；「全选」应覆盖全部候选年或改为「选满上限」。 |

### P2 — 中影响（体验 / 健壮性 / 一致性）

| 编号 | 缺陷 | 位置 | 影响 | 建议 |
|---|---|---|---|---|
| **P2-1** | 曲线显隐状态按 dataset **索引**而非年份标识 | `TempChart.ts:135-147`（`applyHiddenState`）、`:271-272` | 用户隐藏某年份后若**改选年份**（数组顺序/数量变化），同一索引可能映射到不同年份，导致「隐藏错位」。温度标签切换因年份顺序不变故正常。 | 用稳定 key（如 `year`）记录隐藏集合，而非索引。 |
| **P2-2** | 城市选择后不自动查询 + 全局监听未清理 | `CitySearch.ts:25-29`、`CitySearch.ts:85-89` | 选中城市仅更新 `state.city`，需手动点「查询」才生效（与年份/区间选择行为不一致）；`document` 上的 click 监听从不移除（SPA 单例下影响小，但属泄漏）。 | 选中城市后触发 `onSelect` 并自动 `handleQuery`；组件销毁时 `removeEventListener`。 |
| **P2-3** | 跨年分隔行文案写死「1–2 月」 | `DataTable.ts:49` | 跨年区间若只含次年 1 月（或更长到 3 月），文案「以下为次年 1–2 月」不准确。 | 文案改为「以下为次年（year+1）数据」或动态计算跨年段起止月。 |
| **P2-4** | 表内高温着色阈值与统计卡阈值体系不一致 | `utils/helpers.ts:94-98`（`getTemperatureColor` 用 ≥35/≥30）vs `logic/extremes.ts` 的 `TH` 常量 | 详情表里颜色判定（≥30 橙、≥35 红）与统计卡/极端事件卡的国标阈值（≥35 热浪、≥25 热夜等）并非同一套，易让用户困惑。 | 从统一阈值常量派生着色，避免两套硬编码。 |
| **P2-5** | 无请求失败重试 / 部分失败提示弱 | `main.ts:339-347` | 个别年份 429/超时仅以文本横幅提示，无重试入口；用户需手动重查。 | 提供「重试失败年份」按钮，对失败年份指数退避重试。 |

### P3 — 低影响（整洁度 / 可维护性）

| 编号 | 缺陷 | 位置 | 影响 | 建议 |
|---|---|---|---|---|
| **P3-1** | 残留死代码 | `TempChart.ts:282`（`(canvas as any).__chartjs_instance__ = chart`） | 赋值但无读取方，疑似调试遗留。 | 删除。 |
| **P3-2** | 默认区间含当年，可能误导 | `utils/helpers.ts:1-9`（`getRecentYears(..., false)` 含当前年） | 默认选中含「今年」，而今年数据为部分值 + 预报，与历史整年对比时口径不一致。 | 默认年份改为排除当年（`excludeCurrent=true`），或明显标注今年为不完全年。 |
| **P3-3** | 无测试 / CI | `package.json`（仅 dev/build/preview）、无 `.github`/vitest | 逻辑改动（如 `extremes.ts`）无回归保护。 | 引入 vitest，将本次验证脚本固化为单测；加 GitHub Actions 跑 `build` + `test`。 |
| **P3-4** | 错误横幅无类型区分样式细节 | `main.ts:176-180` | `message-error` / `message-info` 仅切换 class，颜色依赖 CSS；无图标/可关闭。 | 可选：增加关闭按钮与更明显视觉。 |

---

## 2. 优先级建议（落地顺序）

1. **P1-2（缓存）+ P1-1（请求取消）**：一起做，直接消除 429 与竞态，是稳定性基础。
2. **P1-3（持久化）**：低成本高体验收益，建议紧随其后。
3. **P1-4（年份上限）**：按需放宽，注意多年份会放大 P1-2 的缓存价值。
4. **P2 系列**：显隐按年份标识（P2-1）、城市选后自动查询（P2-2）是体验短板，建议在本轮补齐。
5. **P3-3（测试/CI）**：把本次验证逻辑沉淀为 vitest 用例，防止回归。

> 注：所有 P0（数据正确性）问题已在先前轮次修复并验证（闰年对齐、跨年归属、当年预报拆分、失败可见性），本轮未复现。
